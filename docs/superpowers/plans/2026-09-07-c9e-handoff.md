# C9e local handoff — 2026-09-07

Status: implementation and fresh local verification complete. Independent whole-branch source review approved through `92f69dd`; final evidence/handoff inspection approved. C9e is locally complete. This is a local checkpoint, not a release or database activation.

## Delivered behavior

Owned approved C9d versions now offer deterministic JSON and plain-text downloads. Historical approved versions remain downloadable; export performs no delivery write. The original content hash identifies frozen review content; the separate export hash identifies the canonical JSON envelope. Downloads use `delivery-<versionUUID>.json|txt`, no-store, nosniff and authenticated attachment responses. Manual delivery is explicitly self-reported, not verified publication or measured impact.

Current account members can record an attestation against the latest approved version, then correct it by appending a reasoned withdrawal and an eligible replacement. Actor, approval identity and recorded time are server-derived. Declared UTC delivery time remains separate and is checked against approval/database time. Exact retries replay immutable history, including withdrawn/superseded events; they never reactivate it. History/capabilities are account scoped and full-history derived, independently of pagination. Existing paid/provider and independent-approval gates remain unchanged.

Bilingual en/zh-HK UI labels timezone/provenance and preserves delivery input and withdrawal reasons through errors, conflicts, delayed reads, pending selection and pending submission. The server remains authoritative for eligibility. No new roles, public sharing, CMS/provider calls, uploads, email, paid scans, outcome jobs or automatic C9f activity are introduced.

## Exact source and review

- Baseline: `12ec30d23654d9c8206d80935a1b7173647fd1d8` (preserved C9d branch).
- Tested application source: `92f69dd85ca029fc6c199b4cd8a0776e4a008274` on `codex/c9e-delivery-implementation`.
- Task7 changes only this handoff, the C9e plan checklist/status and the C9e spec status. The documentation commit is identified by `git log -1 -- docs/superpowers/plans/2026-09-07-c9e-handoff.md`; its exact SHA is retained in `.superpowers/sdd/c9e-task7-report.md` after commit.
- Independent reviewer covered the entire `12ec30d..92f69dd` range, found no additional actionable issue and approved source conditional on fresh verification/truthful handoff. Review record: `.superpowers/sdd/c9e-final-review-report.md`. Final reviewer inspection confirmed the fresh 2,945-unit, 182-browser, 52-fixture and static/build evidence at the exact tested source and approved the overall local handoff with no actionable issues; the initial condition is satisfied. Reviewer remained independent of implementation; reuse was required by agent capacity.

## Fresh verification at the tested source

Every Node command used the ignored sanitized dummy-environment runner; no `.env` files were loaded. Commands sharing `.next` ran sequentially. These current results replace neither prior raw evidence nor unrun external proofs.

| Gate | Result | Raw artifact under `.superpowers/sdd/` |
| --- | --- | --- |
| Full offline units | 257 files, 2,945 tests passed; exit 0 | `c9e-task7-unit.log` / `.meta.json` |
| Full package lint | Exit 0, no diagnostics | `c9e-task7-lint.log` / `.meta.json` |
| Next type generation | Exit 0 | `c9e-task7-typegen.log` / `.meta.json` |
| Full TypeScript | Exit 0, no diagnostics | `c9e-task7-tsc.log` / `.meta.json` |
| Production build, dummy config | Exit 0 | `c9e-task7-build.log` / `.meta.json` |
| All component fixtures | 11 files, 52 tests passed; exit 0 | `c9e-task7-fixtures.log` / `.meta.json` |
| Chromium/mobile C9e+C9d+C9c+auth | 182 passed: C9e 68, C9d 48, C9c 44, auth 22; exit 0, no skips/flaky/failures | `c9e-task7-browser.log`, `.json`, `.meta.json` |

Exact commands and UTC start/end/exit/source metadata are retained in `.superpowers/sdd/c9e-task7-report.md` and per-gate JSON. Browser raw JSON was copied immediately into the unique Task7 artifact. Actual JSON shows 182 expected/passed, 0 unexpected, 0 skipped, 0 flaky and errors []; 91 Chromium and 91 mobile, duration 200305.483 ms. The parsed by-file/project summary is `c9e-task7-browser-summary.json`. The full unit wrapper excludes `__tests__/integration/**` and restores its temporary Playwright ignore change byte-for-byte (SHA256 before/after `6F2126BE11BD892ECC134646CB526F46F1595672AF0A7DBA1DE73A5D9071686F`). Offline C9e isolation tests (2) and C9d isolation tests (3) passed within the full unit run; no SQL-suite configuration was invoked.

Browser verification uses intercepted in-memory fixtures against the local built application. It proves UI/download/request behavior, not live Neon authentication, transaction locks, application grants or provider results. Earlier C9d 2,682-unit/48-browser results and Task6 108/112/116-browser results remain baseline/intermediate history, not fresh Task7 counts.

## Reproduced failures and setup history

Prior Task6 corrections remain covered at the tested source: withdrawal originally erased prepared correction fields; an active record outside the page produced a misleading empty history; a new delivery loading status collided with C9d action-status selectors; and pending version submission could unmount a dirty delivery form. The latter was an independent-review P2 with four observed RED input-loss cases and four matching GREEN cases; `92f69dd` fixes the selection/permission refresh race. Existing C9d assertions were retained.

Initial datetime-local fill failures were test setup failures, not product RED. Initial UI lint issues were corrected before the final source. Task6 captured a mobile screenshot but its full-height image could not be rendered for visual inspection; mobile interactions, axe/focus and overflow checks passed. Task7 uses scoped elevated execution because of the known sandbox ACL-helper constraint; no ACL changes were made. All fresh Task7 gates passed on their first execution; no new product or setup failures occurred.

## Preservation

The published C9d branch remains locally at `12ec30d23654d9c8206d80935a1b7173647fd1d8`; no push, merge or PR mutation occurred. This does not claim a new remote CI/deployment check. Original root checkout work was preserved. The original user plan `C:/Users/laich/Documents/Aiso/docs/superpowers/plans/2026-09-05-aiso-codex-6-astra-continuation.md` still hashes to `6B1C42059CA7BB01422C90B07F6AFCDF05102A426A9DDF9E41958C38581812B3`. Baseline diff check passed. Final staged/committed status is retained in the Task7 report; only the three named documents are staged.

## External evidence and rollback

Migration `043_delivery_attestations.sql` is **UNAPPLIED**. Dedicated C9e constraint/actual-store SQL tests and both C9d SQL suites remain **AUTHORED / UNRUN**. They were typechecked, not executed. Prior prerequisite migration activation, C1 database equivalence and sterile nonproduction topology remain separate gates. No live concurrency, constraint, race, privilege, rollback or target-equivalence result is claimed by mocked tests.

Any future external proposal must name the exact project, disposable branch, database, owner/application role and source/SQL SHAs, verify prerequisites and in-band identities, and specify validation/abort criteria and retention-aware rollback before separate approval. Current project/branch/database/owner/provider target: **UNKNOWN**. `aeo_app` is only the expected application-role contract, not a verified target. No target is inferred from old notes or local environment.

Application rollback removes/disables C9e routes, delivery components/services and their version-workspace integration while preserving C9c/C9d behavior. If activated later, retain all immutable attestation/withdrawal history and disable access/writes; do not drop/delete audit data. C9f comparable rechecks/outcomes, remaining C10 live obligations and C11 release/cutover remain separate scopes. Stop here without publication or external execution.
