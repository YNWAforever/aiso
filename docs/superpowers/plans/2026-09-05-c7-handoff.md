# C7 local implementation handoff

Branch: `codex/c7-scan-results`. Baseline: `1f96315`, which contains C4-C6 from PR #15 and the reviewed PR #14 pricing badge fix. C7 changes are uncommitted. The original user continuation plan remains untracked and excluded from the review patch. No external mutations, provider calls, real email, paid scans, migration application, deployment, publication or merge were performed.

## Delivered behavior

- Localized `/scan` and `/methodology` pages reuse the existing shell, metadata API and scan service. The available public catalog now contains 24 routes and the sitemap exactly 48 localized URLs.
- Submission exposes safe localized URL/rate-limit/quota/auth-service/persistence/general retry messages. Unknown diagnostics are not echoed. Invalid success IDs cannot trigger completion telemetry or result navigation. Pending and partial response routing preserve the existing result journey.
- New diagnostic snapshots use methodology `2026-09-05.v2`. Unrounded weighted coverage below 0.67 suppresses the numeric pillar score as insufficient evidence; 0.67 to below 0.85 is provisional; 0.85 and above is scored. Explicit not-applicable checks remove denominator/numerator weight. Missing, failed, unverifiable or ambiguous partial collection contributes no observed coverage. Zero applicable weight is insufficient.
- Original stored snapshots remain under their original methodology. Historical rows without snapshots are labelled recalculated; absent verifiable evidence produces insufficient diagnostics. No historical rewrite, migration or backfill. Headline scores, grades and weights remain unchanged.
- Owner result details show sanitized collection state, coverage, check assessments and method versions through the existing owner check. Public summaries do not receive the evidence projection or private breakdown. Client bundles do not import the server evidence/crypto module. Current origin-only evidence cannot substantiate an improvement comparison.
- Public and owner impact projections are explicitly labelled estimates/inferences. Existing result layouts, claim routes, claim-intent validation, AuthComplete and revoked-report behavior remain intact.

## Verification evidence

| Check | Result |
| --- | --- |
| Full unit suite | 185 files / 2,034 tests passed; live integrations and isolated checkout copies excluded |
| Focused owner/result/claim compatibility | 50 tests passed before final full suite |
| Focused renderer/estimate regressions | 10 tests passed; final type-safe fixture recheck recorded separately |
| Source ESLint | Passed; generated directories excluded |
| Default Next.js production build | Passed after replacing the fixture dependency junction with copied installed dependencies |
| Route type generation and standalone TypeScript | Passed after the type-safe test-fixture repair |
| Production-fixture browser regression | 236 passed, zero failures or skips (4.9 minutes); both locales/themes and affected desktop/mobile flows |
| Independent review | Accepted after fixing malformed success IDs, unknown400 classification, missing timestamps and public estimate qualification |
| Review patch and whitespace | Passed: whitespace check and combined patch reverse-apply dry run |

Logs: `.codex/c7-unit-final.log`, `c7-build-final.log`, `c7-typecheck.log`, `c7-browser.log`, `c7-lint.log`, `c7-result-claim-green.log`. The initial full unit run overlapped the review fixes' red-test phase; it was rerun after edits stabilized and all 2,034 tests passed. The initial build failure was an isolated Turbopack dependency-junction restriction; the same source built successfully with local dependencies. Standalone typecheck caught a test fixture missing required check fields; that fixture was repaired rather than weakening types.

## Evidence limits and remaining work

This is local synthetic/mocked evidence. Real Auth/signup, live database persistence, billing/providers, customer writes and intended staging-environment equivalence were not exercised. Existing integration and live-service gates remain separate. Accessibility regressions do not establish universal WCAG compliance. Lighthouse remains unavailable and no new Lighthouse/performance claim is made.

C8a source mapping and its proposed outcome-home design are in `.codex/c8a-workspace-home-design.md`, outside this implementation diff. It recommends preserving explicit legacy step links, actual newest-week Pulse queries, server entitlement projection, observation dates with unknown freshness, and per-panel error states. These concrete workspace behavior choices are the next design review. C8b-g, C9 contracts, C10 operational/provider verification and C11 readiness/cutover are not represented as implemented. C9 still needs entity-verification/approval/delivery semantics; C11 still needs named owners, environment/domain/canary/recovery decisions and separate external authorization.

## Review and rollback

`.codex/c7-review.patch` and `.codex/c7-files.json` contain only the C7 scope against `1f96315`, excluding the original user plan. After checking for subsequent user edits, reverse-check and reverse this patch to roll back the local slice. Existing result/claim routes and old stored snapshots remain usable. If new snapshots are ever externally persisted, their v2 compatibility must be considered in a deployed rollback; no deployed/database rollback is authorized or needed for this local handoff.
Local verification completed 2026-09-06 Asia/Hong_Kong. The last change was a type-safe test fixture, whose two focused tests and standalone typecheck passed; production runtime source was unchanged.
