# C9d local verification and handoff

Date: 2026-09-07 (Asia/Hong_Kong). Status: Tasks 1-6 implemented and independently approved; Task 7 local verification and handoff complete, **final independent whole-branch review approved, including the final permission-outage repair**. This does not authorize activation or publication.

Branch: `codex/c9d-change-set-implementation`. Worktree: `C:/Users/laich/Documents/Aiso/.worktrees/c9b-c9c`. Baseline: `195282e70084fb270a4bd7df3e91527869bd4d31`. Latest tested source/test checkpoint: `95b8566ec2d7aaf074e26755335c1fabd178efb3`. The Task7 results below retain their original historical checkpoint `904f5c24ba25c26775341bf3cd339f632fcf1462`; the final review repair and current verification are recorded in the closure addendum.

## Delivered behavior

One immutable review version belongs to one existing editable, draft-only work item. Submission names the persisted revision and preserves the canonical content, hash, evidence limitations and actor snapshot. Same-revision retries replay retained content even after draft edits; reads do not need the original source rows. Designated account approvers can make one terminal decision on the latest version, with a normalized reason; self-decisions and stale/missing authority are denied. Platform administrators manage account approver access through revision-checked state and immutable audit events. The bilingual interfaces preserve unsaved draft/reason input and retry identity; late reads cannot undo a confirmed decision. Approval is review, not delivery, publication or evidence of measured impact.

## Task 7 additions and reproduced repair

- Added a persisted JSON round-trip/history DTO regression in `__tests__/change-sets/store.test.ts`: unknown recorded time and nullable prompt/actor name stay null, while supported retained Pulse provenance stays intact. It asserts no draft or original Pulse/scan read. No unsupported provenance enum or weakened validator was introduced.
- Authored `__tests__/integration/change-set-stores.test.ts` and `vitest.change-set-stores-integration.config.ts`. The suite calls actual submit/read/access/decision stores and the actual draft reader, injecting only separately verified real application SQL into db(). No store result or SQL response is mocked.
- Final harness inspection found the new suite would match generic integration discovery. A source-contract regression reproduced 1 failure/2 passes before the minimal explicit exclusion in `vitest.integration.config.ts`; the same test then passed 3/3. This was a Task 7 test-harness gap, not a product/runtime regression. No database test was executed to reproduce it.
- The initial full suite passed 2681 tests; after the one additional configuration regression and repair, the refreshed full suite passed 2682. Post-repair scoped lint and full TypeScript passed. Build/fixture/browser application source did not change, so no duplicate build was needed for the test-only repair.

## Fresh local verification

All commands ran in the worktree. Every command below has full stdout/stderr in `.superpowers/sdd/c9d-task7-<label>.log` and exact command, timestamps, exitCode and signal in its adjacent `.json`. Execution was wrapped by `node .superpowers/sdd/c9d-task7-run.cjs <label> <arguments after node below>`. The wrapper preserves raw browser JSON immediately after completion, before another reporter can overwrite it. These are retained local artifacts, not tracked CI results.

| Label | Exact command | Exit | Result |
| --- | --- | --- | --- |
| retained | `node .superpowers/sdd/local-run.cjs node_modules/vitest/vitest.mjs run __tests__/change-sets/store.test.ts --maxWorkers=2` | 0 | 18 passed |
| unit | `node .superpowers/sdd/unit-run.cjs` | 0 | 248 files / 2681 passed (before new isolation regression) |
| lint | `node .superpowers/sdd/local-run.cjs node_modules/eslint/bin/eslint.js . --ignore-pattern .claude/ --ignore-pattern .superpowers/ --ignore-pattern .codebase-memory/** --ignore-pattern coverage/ --ignore-pattern cloudflare/** --ignore-pattern .playwright-ci-server/**` | 0 | 0 errors, 0 warnings |
| typegen | `node .superpowers/sdd/local-run.cjs node_modules/next/dist/bin/next typegen` | 0 | Route types generated |
| tsc | `node .superpowers/sdd/local-run.cjs node_modules/typescript/bin/tsc --noEmit` | 0 | No diagnostics |
| build | `node .superpowers/sdd/local-run.cjs node_modules/next/dist/bin/next build` | 0 | Next 16.2.4; compile 17.7s; TypeScript 26.1s; 15 static pages |
| fixtures | `node .superpowers/sdd/local-run.cjs scripts/ci/prepare-component-fixtures.mjs` | 0 | 10 files / 47 passed |
| integration-isolation-red | `node .superpowers/sdd/local-run.cjs node_modules/vitest/vitest.mjs run __tests__/config/change-set-integration.test.ts --maxWorkers=2` | 1 | Expected RED: 1 failed / 2 passed; new store suite missing from generic exclude |
| integration-isolation-green | `node .superpowers/sdd/local-run.cjs node_modules/vitest/vitest.mjs run __tests__/config/change-set-integration.test.ts --maxWorkers=2` | 0 | 3 passed |
| isolation-lint | `node .superpowers/sdd/local-run.cjs node_modules/eslint/bin/eslint.js vitest.integration.config.ts __tests__/config/change-set-integration.test.ts` | 0 | No diagnostics |
| tsc-final | `node .superpowers/sdd/local-run.cjs node_modules/typescript/bin/tsc --noEmit` | 0 | No diagnostics |
| browser | `node .superpowers/sdd/local-run.cjs node_modules/playwright/cli.js test tests/e2e/c9d-change-sets.spec.ts tests/e2e/c9c-opportunities.spec.ts tests/e2e/c9b-observations.spec.ts tests/e2e/auth.spec.ts --config .superpowers/sdd/playwright.local.config.cjs --project=chromium --project=mobile` | 0 | 134 passed; 0 failed / skipped / flaky; 133.303s raw duration |
| unit-final | `node .superpowers/sdd/unit-run.cjs` | 0 | 248 files / 2682 passed; 0 failed; 129.24s Vitest duration |

The final unit command expands through unit-run.cjs to `node .superpowers/sdd/local-run.cjs node_modules/vitest/vitest.mjs run --exclude __tests__/integration/** --maxWorkers=2`. Integration was excluded, not counted as skipped tests. The temporary removal of the enclosing-worktree Playwright ignore was restored in finally; `git diff --exit-code -- playwright.config.ts` passed after both runs. No concurrent config edit occurred.

The package lint command uses the exact package.json ignore scope, not bare eslint over ignored tooling. local-run.cjs strips ambient environment to OS essentials and injects local dummy URLs/secrets and fixture paths; no .env was read or changed. Browser runs use a localhost production server, hydrated component fixtures/mocked HTTP, and in-memory auth fixtures; they do not demonstrate live database authorization or send real magic-link email.

Full selected browser files, no grep: C9d 48 + C9c 44 + C9b 20 + auth 22 = 134; Chromium/mobile, locale cases as defined in the full files. Raw report: `.superpowers/sdd/c9d-task7-browser-browser-results.json` (134 expected, 0 unexpected/skipped/flaky, errors []). Derived per-file summary: `.superpowers/sdd/c9d-task7-browser-summary.json`. No selected browser failure or environmental setup failure occurred.

The initial sandbox shell launch failed with `apply deny-read ACLs`; scoped require_escalated shell execution was used thereafter. No ACL changes were made. Missing exact discovery paths were resolved by reading the current filenames; no architecture was inferred from stale graph results.

## Database evidence and review boundary

Both dedicated suites are **AUTHORED / UNRUN**; migration `042_change_set_approvals.sql` is **UNAPPLIED here; target UNKNOWN**. No live SQL, database provisioning, grants, migration, credentials/environment mutation, customer write, paid scan, email, push, merge or deployment occurred.

The existing structural suite/config remain `__tests__/integration/change-set-approvals.test.ts` / `vitest.change-sets-integration.config.ts`. They cover actual SQL constraints, grants, append-only history, invalid audit rollback, profile deletion snapshots, parent restrictions and raw lock protocol. They are not actual-store execution evidence.

The new dedicated store suite authors 15 cases: concurrent first-grant CAS/audit uniqueness; exact access replay and altered-payload conflict; admin demotion and moved/deleted target after precheck; moved/deleted submitter after draft pre-read; concurrent submission and replay after edits; overlapping decision/revoke requests with both commit orderings and exact decision retry; moved/deleted reviewer after retained pre-read; newer-version supersession after pre-read; injected serialization/deadlock retry codes followed by real persistence. Pauses occur before actual transaction dispatch to deterministically exercise post-pre-read authority checks; the concurrent first-grant/submission cases use actual overlapping database transactions when later authorized. Injected 40001/40P01 errors test retry orchestration and do not claim a real database-generated deadlock.

Before fixture writes, the new suite requires explicit `C9D_DISPOSABLE_PROJECT_ID`, `C9D_DISPOSABLE_BRANCH_ID`, owner `C9D_TEST_DATABASE_URL`, and separate application `C9D_TEST_APP_DATABASE_URL`. It rejects partial opt-in/protected targets, verifies both server project/branch identities, owner capability/schema presence and application current_user=aeo_app. db() stays closed until those read-only identity checks complete. No setup, provisioning, migration, env-file loading or provider cleanup is attached; the generic integration config excludes both C9d suites. Immutable test history is intentionally retained; target disposal needs separate authorization.

Local source inspection confirmed the intended profile -> item -> grant lock order, ordered access target locks protecting absent state, initialized transaction witnesses, exact nullable/composite SQL bindings, INSERT SELECT revision/content comparison, latest-version decision check, owned replay before new eligibility, bounded retry handling, and explicit SELECT/INSERT-only history privileges with restricted parent deletion. Actor snapshots have no profile-deletion cascade. These source findings and mocked unit contracts do not substitute for unrun PostgreSQL execution or the final independent whole-branch review. The controller subsequently inspected the final repair checkpoint and raw evidence and confirmed independent review approval, as recorded in the closure addendum.

## Preservation, rollback and future activation

Original root checkout and user plan are preserved. SHA-256 of `C:/Users/laich/Documents/Aiso/docs/superpowers/plans/2026-09-05-aiso-codex-6-astra-continuation.md`: `6B1C42059CA7BB01422C90B07F6AFCDF05102A426A9DDF9E41958C38581812B3` (rechecked locally). Explicit staging only; no reset/clean of user work. Source checkpoint was clean after commit. Baseline and staged whitespace checks passed; final HEAD checks are recorded in the Task 7 local report.

Source rollback, if later approved: use a reviewed revert of the C9d commit range in an isolated branch to restore the baseline behavior while retaining C9b/C9c. Do not reset the user's checkout. No database rollback is needed for this local run because no migration was applied. If activated later, immutable history requires a separately designed retention-aware rollback; do not drop history tables as a source rollback shortcut.

Future activation requires separate explicit authorization in this order:

1. Final whole-branch review and affected repair verification are complete locally; retain the approved source checkpoint for any separately authorized activation.
2. Name and approve an exact disposable project/branch/database, verify live server identities and least-privilege application role, and inspect the migration ledger. Do not infer a target from local environment files.
3. Separately authorize preparation/application of 042 on that exact disposable target and supply the dedicated owner/application test bindings securely.
4. Run both dedicated configurations, capture actual PostgreSQL constraints/privileges/race/retry/retention evidence, and separately authorize disposal of the fixture target.
5. Treat any production migration, grants, deployment or publication as a new exact-target approval; none is authorized by this handoff.

## Final review repair and closure (2026-09-07)

Exact source/test commit: `95b8566ec2d7aaf074e26755335c1fabd178efb3`, following reviewed checkpoint `1b86899261d03fa5fe638d49b20d5c43714adc21`. The sole final P2 was reproduced before the fix: a successful approver-access read followed by HTTP503 retained mutation authority. Refresh now invalidates authority immediately and restores it only after a successful JSON response. Previously verified forms remain visible through503/network/JSON outages, with mutations disabled and reason/member/action/request identity retained. Explicit401/403 still hides the form. Server-side authorization is unchanged.

The expanded bilingual browser case verifies503, network abort, invalid JSON, successful recovery and identical retry POST payload/requestId. RED was4 expected assertion failures (enabled save); GREEN is the complete C9d file,48 passing Chromium/mobile cases. The parent independently inspected raw reports and received a read-only final-review recheck: P2 resolved, no new findings. Final independent review is approved; local implementation and verification are complete. This is not live database or deployment approval.

| Label | Exact command | Exit | Result |
| --- | --- | --- | --- |
| final-fix-red | `node .superpowers/sdd/local-run.cjs node_modules/playwright/cli.js test tests/e2e/c9d-change-sets.spec.ts --grep admin independent cursors --config .superpowers/sdd/playwright.local.config.cjs --project=chromium --project=mobile` (the grep argument was quoted as `"admin independent cursors"`) | 1 | Expected RED: 4 failed; save button received enabled after 503; both locales and projects |
| final-fix-lint-final | `node .superpowers/sdd/local-run.cjs node_modules/eslint/bin/eslint.js components/approvals/ApproverAccessWorkspace.tsx tests/e2e/c9d-change-sets.spec.ts` | 0 | No diagnostics |
| final-fix-unit | `node .superpowers/sdd/unit-run.cjs` | 0 | 248 files / 2682 passed; 95.66s; integration excluded |
| final-fix-build-serial | `node .superpowers/sdd/local-run.cjs node_modules/next/dist/bin/next build` | 0 | Next16.2.4; compile13.4s; TypeScript25.4s; 15 pages |
| final-fix-fixtures | `node .superpowers/sdd/local-run.cjs scripts/ci/prepare-component-fixtures.mjs` | 0 | 10 files / 47 passed |
| final-fix-browser | `node .superpowers/sdd/local-run.cjs node_modules/playwright/cli.js test tests/e2e/c9d-change-sets.spec.ts --config .superpowers/sdd/playwright.local.config.cjs --project=chromium --project=mobile` | 0 | 48 passed; 0 unexpected/skipped/flaky; errors []; 63.920s raw duration |
| final-fix-tsc | `node .superpowers/sdd/local-run.cjs node_modules/typescript/bin/tsc --noEmit` | 0 | No diagnostics |

Full stdout/stderr and command/timestamp/exit metadata are retained under `.superpowers/sdd/c9d-task7-<label>.log` / adjacent `.json`; each command was launched with `node .superpowers/sdd/c9d-task7-run.cjs <label> <arguments after node>`. The wrapper immediately preserved raw browser reports as `c9d-task7-final-fix-red-browser-results.json` and `c9d-task7-final-fix-browser-browser-results.json` in the same directory. The full unit wrapper restored its temporary Playwright ignore change; `git diff --exit-code -- playwright.config.ts` and scoped whitespace checks passed.

An intermediate build `final-fix-build-final` failed exit1 with ENOTEMPTY on `.next/component-fixtures` while the full unit fixture generator overlapped build cleanup. Its raw log/metadata remain retained. After the unit run finished, serialized final build, all fixtures and browser verification passed; no product failure or ACL change was inferred. Earlier `final-fix-build` and `final-fix-build-current` were superseded intermediate build evidence. No application edits followed the serialized final build.

The original134-case C9b/C9c/auth/C9d run remains historical evidence at its recorded checkpoint; only the full48-case C9d browser file was rerun for this scoped fix. Full unit2682, standalone TypeScript, scoped lint, fresh build and all47 fixture tests are current. Both dedicated PostgreSQL suites remain AUTHORED/UNRUN; migration042 remains UNAPPLIED here, target UNKNOWN. No live provider/database/environment/credential mutation, migration, paid action, email, push, merge or deployment occurred.
