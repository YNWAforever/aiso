# C9a / C10 / C11 local implementation handoff — 2026-09-06

## Review identity

Branch: `codex/c9a-private-entities`. Base: `5aabacd16d338afc15fbaa3d5db62641d7b5c628` (preserved C8c–g checkpoint). Implementation is uncommitted and ready for review; nothing was pushed, merged or deployed.

Exact combined patch: `.codex/c9a-review.patch`; explicit manifest: `.codex/c9a-files.json`. Separate C10 runtime/test patch: `.codex/c10-review.patch`, manifest `.codex/c10-files.json`. These ignored local artifacts are generated with temporary indexes; the real index remains unchanged. The combined patch includes C9a, bounded C10 repairs, regression configuration and readiness documentation.

The original untracked continuation plan is excluded and unchanged: SHA256 `6B1C42059CA7BB01422C90B07F6AFCDF05102A426A9DDF9E41958C38581812B3`.

## Changed behavior

C9a adds one private, explicitly unverified entity record per owned existing client, with canonical display name and normalized aliases. GET never writes. The bilingual editor distinguishes unsaved suggestions from saved data, preserves drafts on failures/conflicts, and requires explicit reload to discard a conflicting draft. Revision compare-and-set prevents overwrites; matching newer stored content acknowledges a lost-response retry without incrementing again. Ownership checks remain server-side. This does not rename the existing client, change scans, expose public entities or introduce roles.

Migration040 is additive source only. Its exact-target integration test has no provisioning hooks, is excluded from normal integration setup, and requires explicit identity/ownership checks before fixture writes. It has not been run and the SQL has not been applied.

C10 replaces raw cron-ledger database diagnostics with allowlisted fields, marks three Pulse failure outcomes as errors while preserving HTTP behavior, and strengthens disposable branch registration/deletion and default/primary pruner exclusions. Playwright respects ignored directories during traversal; all450 real discovery entries remain, and100 ignored patch-fixture duplicates are excluded.

C11 is the readiness dossier in `2026-09-06-c10-c11-continuation-inventory.md`, not an executed cutover. Topology values are recorded intentions, not newly verified live bindings.

## Checks actually run

- Full offline root regression suite:210 files,2248 tests passed (`.codex/c9a-unit.log`). Database integration excluded.
- Final isolated Next16.2.4 production build, TypeScript `--noEmit`, and full source lint passed (`.codex/c9a-build.log`, `c9a-tsc.log`, `c9a-lint.log`). Build generated route types. Only dummy configuration was supplied.
- Final hydrated entity browser acceptance:26 passed in17.8s (`.codex/c9a-browser.log`), using final production CSS, mocked requests and in-memory fixtures. Covers both locales,375/1440 widths, light/dark, keyboard/accessibility, save/conflict/reload/network/retry and stale-response behavior. Fixture generation:2 passed.
- C10 focused checks:71 passed; existing related mock checks:145 passed. Worker mock suite:6 passed via installed root Vitest. These overlap full-suite coverage and are not additional total tests.
- Independent backend and UI spec/quality review passed after fixes. Independent C10 review passed, including exact HTTP assertions and discovery inventory comparison. No remaining reported findings.
- Patch reverse-application check, whitespace check, validated source-copy equivalence and original-plan hash checks passed. Real index unchanged.

## Failures resolved and evidence limits

Initial full suite failed three stale migration-catalog fixtures and two discovery timeout tests. The catalog now includes authored040 without altering historical PostgreSQL evidence; a missing-table guard regression was added. Ignored build traversal caused discovery timeouts; respecting Git ignores fixed both without increasing timeouts or removing actual source tests.

Initial browser fixtures failed from encoding/JSX fixture setup issues; fixed fixture generation and final26-case run passed. Independent review found a valid newer identical-payload retry rejected by the editor; a failing regression was reproduced and matching/newer acceptance plus malformed/stale rejection cases now pass.

Worker-local dependencies were absent; root-installed Vitest ran its mocked tests. No Worker install/typecheck/deployed scheduler proof is claimed. No live database, auth issuer, billing/provider execution, email, paid scan, customer write, cleanup, environment/credential mutation or deployment occurred. Browser fixtures do not prove live database integration.

## Remaining work and rollback

The approved C9a vertical is complete locally. C9b–f remain later work: public verification, products/sub-entities, approval/delivery and attribution contracts must be resolved in their respective slices. C10 live provider/scheduler/credential/cleanup obligations remain unverified. C11 release remains gated on C1 current-head bootstrap/equivalence including040, exact disposable target and app-role proof, sterile nonproduction topology, named owners, canary/write fences and measured recovery evidence.

No executable external action is proposed with guessed targets. A future proposal must pin the exact project/branch/database/role/domain/provider, reviewed source/SQL diff, validation and abort criteria, named owner, and rollback including new writes. Runtime activation requires schema040 first. Application rollback should retain the additive table/data; destructive schema rollback requires separate authorization and a retained-data plan.

For this local handoff, retain the patch and revert only its manifest paths if rollback is later requested; preserve the user's continuation plan. No rollback or cleanup was performed. Stop here without starting the next product vertical.
