# C4–C6 local implementation handoff — 2026-09-05

Status: implementation and independent review complete; final browser result recorded below. Local branch `codex/c4-c6-public-pages-evidence`, based on `c123ac254d44a179b7d2f19a19d400ff1cd657b3`. Changes remain uncommitted. The original user continuation plan is preserved and excluded from exported patches. No push, merge, deployment, environment/credential/database/provider mutation, real email, paid scan or customer write was performed.

## Changed behavior

- C4/C5a–e: 20 explicit Server Component public pages, bringing the public navigation catalog to 22 available routes and the derived sitemap to exactly 44 localized URLs. English and Traditional Chinese Hong Kong copy has page-specific metadata, supported calls to action, evidence limitations and Planned labels for unavailable capabilities. Existing metadata API, canonical URL policy and shell tokens remain in use.
- Header: mobile disclosure with keyboard access, Escape/focus restoration, route-change closure, locale-preserving navigation and exact active links; desktop grouped disclosures remain keyboard accessible. Public page text wraps at 200% text size on a 375px viewport.
- C5f: complete bilingual privacy and terms drafts plus separate legal-review questions in `docs/legal/`; no published legal routes. Operator identity, contact, retention, provider inventory, jurisdiction and acceptance decisions remain release gates.
- C5g: bare and localized 308 aliases resolve only to implemented pages; locale and query preservation are covered. Existing temporary signed-report links and report 404 behavior remain unchanged.
- C6: new scans write bounded, versioned `results.evidence` alongside checks and the existing pillar snapshot in the same insert. Collection reliability and assessment are separate. Runtime allowlists, origin-only URL descriptors, zero raw excerpts, 32 KiB envelope/1 KiB check caps and tolerant historical reads prevent unsafe or misleading evidence. Current origin-only evidence refuses improvement comparisons because final path identity is withheld. No migration/backfill or extra provider requests.
- Robots: versioned root-path evaluation corrects grouped agents, wildcard/default rules, longest-match and Allow ties, with explicit crawler-role documentation. Score weights and grade formulas remain unchanged; some robots verdicts intentionally change under the corrected rules.

## Verification

| Check | Actual result |
| --- | --- |
| Full local unit suite | 181 files, 1,975 tests passed; integration tests excluded |
| Source ESLint | Passed |
| Next route type generation and TypeScript | Passed |
| Default Next.js production build | Passed using isolated fixture configuration |
| Six incremental public-family production builds | All passed; each also passed its available-page smoke/accessibility check |
| Combined fixture browser suite | 280 passed, 6 legacy Supabase-dependent email-gate cases skipped; zero failures (12.7 minutes) |
| Open navigation accessibility | 16 full-page axe scans; zero violations |
| Tested source equivalence | 509 source/test/config files compared; zero mismatches |
| Independent review | Public pages, aliases/legal drafts, evidence and robots reviewed; actionable findings fixed and accepted |
| Whitespace and patch applicability | Passed: git diff --check and reverse-apply dry run of the final combined patch |

The browser matrix covers English/zh-HK, light/dark and 375/768/1024/1440 widths, public pages, navigation, redirects and existing authentication/onboarding/result/pricing accessibility regressions. API/provider calls were mocked or blocked; this is local fixture evidence, not live-service acceptance. Accessibility baselines were not raised. Mobile-menu and Chinese desktop screenshots were visually inspected.

Primary logs and measurements are local under `.codex/`: `c4-c6-unit-final.log`, `c4-c6-lint-final.log`, `c4-c6-build-final.log`, `c4-c6-typecheck-final.log`, `c4-c6-browser-final.log`, `navigation-axe.json`, `family-verification.log` and `family-0-performance.json` through `family-5-performance.json`.

## Performance and setup limits

Cold-cache Chromium production transfers on the same fixture origin: home JavaScript 171,481 → 172,024 bytes (+543; 0.32%), pricing 173,246 → 173,789 (+543; 0.31%), stylesheet 16,801 → 16,803 (+2). Final representative new page transferred 168,918 JavaScript bytes. These are below the 10% review trigger. All recorded representative CLS values were zero.

Browser LCP samples were collected but differing server warmth and concurrent local load prevent a defensible before/after LCP conclusion. Lighthouse is not installed; no Lighthouse scores are claimed. Live database/provider, paid scan, email and environment-equivalence checks were not run. Existing live-scan/client-report gates remain excluded, and legacy Supabase-dependent email-gate skips are reported with the final browser result. C1 bootstrap/equivalence repairs are outside this diff.

Review caught and fixed: malformed runtime signal values bypassing envelope limits; required HTML collection errors incorrectly marked complete; an llms.txt fallback incorrectly marked complete after failed reads; robots wildcard/specificity edge cases; and oversized public text overflow. One browser assertion initially matched a same-path locale link and was corrected to target the actual navigation link. None of these are unresolved failures.

## Review artifacts and rollback

`.codex/c4-c6-review.patch` is the combined final patch against the pinned base; `.codex/c4-c6-files.json` lists included files. Public family deltas are `c4-review.patch`, `c5a-review.patch` through `c5e-review.patch`; additional review partitions are `c5f-review.patch`, `c5g-review.patch`, `c6-contract-review.patch`, `c6-capture-review.patch` and `c6-robots-review.patch`. Shared catalog/version dependencies mean partitions are for ordered review, not independent cherry-picking. Browser regression files are included in the combined patch.

For local rollback, first confirm the checkout still matches this handoff and preserve any subsequent user edits. Reverse only the combined patch after a dry-run check; it excludes the original continuation plan. No external rollback is needed because no external state was changed. Any later publication requires its own exact target, reviewed diff, validation and rollback approval. Stop here; C7 and subsequent product slices have not been started.