# C9a implementation plan

> Use superpowers:subagent-driven-development for implementation and independent review; verification-before-completion applies.

Goal: private entity/alias records for existing owned clients, with honest unsaved/conflict states.
Architecture: schema/store/service own tenant and revision invariants; thin route and server page; localized client editor. No public entity claims or new roles. Stack: installed Next16, Neon tagged SQL, React19, next-intl, Vitest/Playwright.

Approved design: ../specs/2026-09-06-c9a-private-entities-design.md. Preserve checkpointed C8 and original user plan. No migration application, external/database/provider/credential/customer write, deployment or publication. Create migration and guarded integration test locally only.

- [x] Task 1 backend: add lib/entities/schema.ts, store.ts, service.ts; migration040 and app/api/clients/[clientId]/entity/route.ts. Tests in __tests__/lib/entities/{schema,store}.test.ts, __tests__/api/client-entity.test.ts, __tests__/migrations/client-entities.test.ts and guarded integration file. Write failing normalization/ownership/no-query/concurrent-write/replay/error tests, implement, run focused then independent review. Interface: Entity={clientId,displayName,aliases,revision,verification:'unverified',updatedAt}; Input={displayName,aliases,expectedRevision}; loadAuthenticatedEntityPage(clientId), saveAuthenticatedEntity(clientId,request), owned client info for page. Explicit API errors from approved design. Example acceptance: expect(normalizeEntityInput({displayName:' Brand ',aliases:['brand','Alias','alias'],expectedRevision:0})).toEqual({displayName:'Brand',aliases:['Alias'],expectedRevision:0}). Use node .codex/c7-unit.cjs with exact test paths, expect red then pass.
- [x] Task 2 UI: components/entities/EntityEditor.tsx, app/[lang]/dashboard/[clientId]/entities/page.tsx, existing workspace navigation, messages locales, page/interaction/renderer tests and tests/e2e/c9a-entities.spec.ts. Read installed page/route/error guides first. Test unsaved suggestion does not imply stored row; conflict preserves draft and requires reload; network failures keep input; duplicate pending submits blocked; client switching cannot install stale response. Both languages and responsive keyboard/axe checks. Root merges translation payloads to prevent shared-file conflicts.
- [x] Task 3 independent task review for spec and quality, resolve all material findings with regressions. Prepare exact diff for reviewer with temporary git index; no real-index stage-all.
- [x] Task 4 C10 source audit: inspect scheduler, ledger, preview cleanup and existing tests; only implement demonstrated deterministic local defects after specific contract review. New provider/live obligations remain gated. C11: update inventory with exact local source SHA/diff and readiness matrix; unknown live evidence stays unknown.
- [x] Task 5 verification: full offline suite, source lint, isolated dummy-env Next production build, typegen/tsc, bilingual browser fixture acceptance; guarded DB integration test is not run. Exact patch/manifest, reverse check, source equivalence and original-plan hash. Record initial failures, remaining local/live blockers and rollback. Stop at local handoff.
